{{- if and .Values.persistence.enabled (eq .Values.kind "Deployment") }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "plik.fullname" . }}
  labels:
    {{- include "plik.labels" . | nindent 4 }}
spec:
  accessModes:
    {{- toYaml .Values.persistence.accessModes | nindent 4 }}
  resources:
    requests:
      storage: {{ .Values.persistence.size }}
  {{- if .Values.persistence.storageClassName }}
  storageClassName: {{ .Values.persistence.storageClassName }}
  {{- end }}
{{- end }}
{{- if and .Values.dbPersistence.enabled (eq .Values.kind "Deployment") }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "plik.fullname" . }}-db
  labels:
    {{- include "plik.labels" . | nindent 4 }}
spec:
  accessModes:
    {{- toYaml .Values.dbPersistence.accessModes | nindent 4 }}
  resources:
    requests:
      storage: {{ .Values.dbPersistence.size }}
  {{- if .Values.dbPersistence.storageClassName }}
  storageClassName: {{ .Values.dbPersistence.storageClassName }}
  {{- end }}
{{- end }}
